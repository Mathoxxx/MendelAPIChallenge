version: '3.8'

services:
  transactions-api:
    build: .
    ports:
      - "8080:8080"
    container_name: mendel-transactions-api
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=prod
